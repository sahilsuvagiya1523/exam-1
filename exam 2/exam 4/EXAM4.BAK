#include <stdio.h>
#include <coino.h>

main()
{
int a [10],i,n;

printf("enter sixe:");
scanf("%d,&n");

for(i=0; i < n; i++)
{
scanf("%d",&a[i]);
}
printf("negative elements:");
for(i=0; i <n; i++)
{
if(a[i]<0)
{
printf("%d",a[i]);
}
}
getch();
}